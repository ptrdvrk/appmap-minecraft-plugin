name: ${name}
version: ${version}
description: ${description}
author: ${author}
website: ${url}

main: ${mainClass}

commands:
    appmap:
        description: AppMap Recorder
        permission: appmapplugin.appmap
        usage: /<command>

permissions: {}